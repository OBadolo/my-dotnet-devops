# My .NET DevOps Project

This project contains a dockerized .NET 9 API with a PostgreSQL database.

## How to run the project locally
1. Make sure Docker is installed.
2. Run the following command:
   ```bash
   docker-compose up -d



--------

# My .NET DevOps Project

Ce projet contient une API .NET 9 dockerisée avec une base de données PostgreSQL.

## Comment lancer le projet localement
1. Assurez-vous d'avoir Docker installé.
2. Lancez la commande suivante :
   ```bash
   docker-compose up -d
